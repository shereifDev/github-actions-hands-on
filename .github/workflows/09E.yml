name: Exercise 

on: 
  workflow_dispatch: 

jobs:
  job_1:
    runs-on: ubuntu-latest
    steps:
      - name: setup python 
        uses: actions/setup-python@v7.0.0
        with:
          python-version: 3.13

      - name: check python version
        run: python --version
            
      
