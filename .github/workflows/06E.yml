name: Exercise 6 

on: 
  workflow_dispatch: 

jobs:
  job_1:
    runs-on: ubuntu-latest
    steps:
      - name: checking disk space 
        run:  df -h    
      - name: checking the date
        run:  date  
      - name: printing the OS uptime
        run:  uptime 
