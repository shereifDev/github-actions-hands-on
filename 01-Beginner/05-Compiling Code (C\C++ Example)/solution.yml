
name: Exercise 5 

on: 
  workflow_dispatch: 

jobs:
  job_1:
    runs-on: ubuntu-latest
    steps:
      - name: clone my repo that contains the code I need to compile it
        uses: actions/checkout@v7.0.1
        
      - name: compile the code using gcc compiler 
        working-directory: ./usedFiles
        run: gcc main.c -o app
       
      - name: run the compiled program 
        run: ./usedFiles/app 
